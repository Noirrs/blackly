const chalk = require('chalk')
const fs = require('fs')
const secrets = require('../../../secrets.json')
module.exports = {
    name: "instagram login",
    usage: `${secrets.configuration.cliName} help`,
    description: "Shows this command list",
    run: function(command){
    let commands;
}
}